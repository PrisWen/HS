<phone:PhoneApplicationPage
    x:Class="HS.ContenidoAlimentacion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <StackPanel  Margin="12,17,0,28">
            <StackPanel Margin="10,10,0,300">
                <TextBlock Name="TituloAlimentacion" Foreground="White" Text="Titulo" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
                <TextBlock Text="tip 1."/>
                <TextBlock Text="tip 2."/>
                <TextBlock Text="tip 3."/>
                <TextBlock Text="tip 4."/>
            </StackPanel>

            <Button Width="170" HorizontalAlignment="Right">Opciones</Button>
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>